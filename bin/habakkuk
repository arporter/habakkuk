#!/usr/bin/env python

''' Top-level driver script for Habakkuk. '''

import sys
from habakkuk.make_dag import runner

if __name__ == "__main__":
    try:
        runner(sys.argv[1:])
    except IOError as err:
        print str(err)
        exit(1)
